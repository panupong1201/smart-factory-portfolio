import { projects } from "@/data/projects";
import { notFound } from "next/navigation";
import Image from "next/image";
import PageTransition from "@/components/PageTransition";
import { ArrowLeft, ArrowRight, Calendar, Clock, User, CheckCircle, AlertCircle, Layers, Database, Cpu, Wifi, Monitor, Play, Youtube } from "lucide-react";
import Link from "next/link";

type Props = {
  params: Promise<{ slug: string }>;
};

export default async function ProjectPage({ params }: Props) {
  const { slug } = await params;
  const projectIndex = projects.findIndex((p) => p.slug === slug);
  const project = projects[projectIndex];

  if (!project) return notFound();

  const nextProject = projects[(projectIndex + 1) % projects.length];

  return (
    <PageTransition>
      <main className="bg-[#050505] text-white min-h-screen pb-24">
        
        {/* 1. Hero Section */}
        <div className="relative h-[60vh] w-full">
            <Image
                src={project.image}
                alt={project.title}
                fill
                className="object-cover opacity-30"
                priority
            />
            <div className="absolute inset-0 bg-gradient-to-t from-[#050505] via-[#050505]/60 to-transparent" />
            <div className="absolute inset-0 bg-gradient-to-r from-[#050505]/80 to-transparent" />
            
            <div className="absolute bottom-0 left-0 w-full p-6 md:p-12">
                <div className="max-w-5xl mx-auto">
                    <Link href="/#projects" className="inline-flex items-center gap-2 text-gray-400 hover:text-white mb-6 transition-colors group">
                        <ArrowLeft className="w-4 h-4 group-hover:-translate-x-1 transition-transform" /> Back to Projects
                    </Link>
                    
                    <h1 className="text-4xl md:text-6xl font-bold text-white mb-6 leading-tight max-w-3xl">
                        {project.title}
                    </h1>
                    
                    <div className="flex flex-wrap gap-2">
                        {project.tech.map((tech: string) => (
                            <span key={tech} className="px-3 py-1 bg-blue-500/10 border border-blue-500/20 text-blue-300 rounded-full text-xs font-mono backdrop-blur-md">
                                {tech}
                            </span>
                        ))}
                    </div>
                </div>
            </div>
        </div>

        <div className="max-w-5xl mx-auto px-6 relative z-10 -mt-10">
            
            {/* 2. Project Stats Bar */}
            <div className="grid grid-cols-2 md:grid-cols-4 gap-4 bg-gray-900/80 backdrop-blur-xl border border-white/10 p-6 rounded-2xl shadow-xl mb-16">
                <div>
                    <div className="flex items-center gap-2 text-gray-400 text-xs font-mono uppercase mb-1">
                        <User className="w-4 h-4" /> Role
                    </div>
                    <p className="font-semibold text-white">{project.role || "Lead Engineer"}</p>
                </div>
                <div>
                    <div className="flex items-center gap-2 text-gray-400 text-xs font-mono uppercase mb-1">
                        <Calendar className="w-4 h-4" /> Year
                    </div>
                    <p className="font-semibold text-white">{project.year || "2024"}</p>
                </div>
                <div>
                    <div className="flex items-center gap-2 text-gray-400 text-xs font-mono uppercase mb-1">
                        <Clock className="w-4 h-4" /> Duration
                    </div>
                    <p className="font-semibold text-white">{project.duration || "Ongoing"}</p>
                </div>
                <div>
                    <div className="flex items-center gap-2 text-gray-400 text-xs font-mono uppercase mb-1">
                        <Layers className="w-4 h-4" /> Type
                    </div>
                    <p className="font-semibold text-white">Industrial Automation</p>
                </div>
            </div>

            {/* 3. The Challenge vs The Solution */}
            <div className="grid md:grid-cols-2 gap-8 mb-20">
                <div className="bg-red-900/5 border border-red-500/10 p-8 rounded-2xl relative overflow-hidden group">
                    <div className="absolute top-0 right-0 p-4 opacity-10 group-hover:opacity-20 transition-opacity">
                        <AlertCircle className="w-24 h-24 text-red-500" />
                    </div>
                    <div className="relative z-10">
                        <div className="flex items-center gap-3 mb-4">
                            <div className="p-2 bg-red-500/20 rounded-lg text-red-400">
                                <AlertCircle className="w-6 h-6" />
                            </div>
                            <h3 className="text-2xl font-bold text-white">The Challenge</h3>
                        </div>
                        <p className="text-gray-300 leading-relaxed text-lg">
                            {project.problem || "Information unavailable."}
                        </p>
                    </div>
                </div>

                <div className="bg-blue-900/5 border border-blue-500/10 p-8 rounded-2xl relative overflow-hidden group">
                    <div className="absolute top-0 right-0 p-4 opacity-10 group-hover:opacity-20 transition-opacity">
                        <CheckCircle className="w-24 h-24 text-blue-500" />
                    </div>
                    <div className="relative z-10">
                        <div className="flex items-center gap-3 mb-4">
                            <div className="p-2 bg-blue-500/20 rounded-lg text-blue-400">
                                <CheckCircle className="w-6 h-6" />
                            </div>
                            <h3 className="text-2xl font-bold text-white">The Solution</h3>
                        </div>
                        <p className="text-gray-300 leading-relaxed text-lg">
                            {project.solution || "Our solution integrates IoT sensors..."}
                        </p>
                    </div>
                </div>
            </div>

            {/* 🔥 [NEW] Before & After Comparison 🔥 */}
            {project.compareImages && (
                <div className="mb-24">
                    <h3 className="text-2xl font-bold text-white mb-8 text-center">Transformation Evidence</h3>
                    <div className="grid md:grid-cols-2 gap-6 relative">
                        {/* BEFORE */}
                        <div className="group relative rounded-2xl overflow-hidden border border-red-500/30">
                            <div className="absolute top-4 left-4 z-10 bg-red-600 text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg">
                                BEFORE: Manual / Legacy
                            </div>
                            <div className="relative h-64 md:h-80 grayscale group-hover:grayscale-0 transition-all duration-500">
                                <Image 
                                    src={project.compareImages.before} 
                                    alt="Before implementation" 
                                    fill 
                                    className="object-cover"
                                />
                                <div className="absolute inset-0 bg-red-900/20 mix-blend-multiply" />
                            </div>
                        </div>

                        {/* AFTER */}
                        <div className="group relative rounded-2xl overflow-hidden border border-green-500/30 shadow-[0_0_30px_rgba(34,197,94,0.2)]">
                            <div className="absolute top-4 left-4 z-10 bg-green-600 text-white text-xs font-bold px-3 py-1 rounded-full shadow-lg flex items-center gap-2">
                                <CheckCircle className="w-3 h-3" /> AFTER: Automated System
                            </div>
                            <div className="relative h-64 md:h-80">
                                <Image 
                                    src={project.compareImages.after} 
                                    alt="After implementation" 
                                    fill 
                                    className="object-cover group-hover:scale-105 transition-transform duration-700"
                                />
                                <div className="absolute inset-0 bg-green-500/10 mix-blend-overlay opacity-0 group-hover:opacity-100 transition-opacity" />
                            </div>
                        </div>
                        
                        {/* Arrow Icon Center (Desktop Only) */}
                        <div className="hidden md:flex absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-12 h-12 bg-[#050505] border border-white/20 rounded-full items-center justify-center z-20">
                            <ArrowRight className="w-6 h-6 text-white" />
                        </div>
                    </div>
                </div>
            )}

            {/* 🔥 [NEW] Video Demo Section 🔥 */}
            {project.videoDemo && (
                <div className="mb-24">
                    <div className="flex items-center justify-between mb-8">
                        <h3 className="text-2xl font-bold text-white flex items-center gap-3">
                            <div className="w-3 h-3 bg-red-500 rounded-full animate-pulse" />
                            System Demo
                        </h3>
                    </div>
                    
                    <div className="relative aspect-video rounded-2xl overflow-hidden border border-white/10 bg-gray-900 shadow-2xl group">
                        {/* ถ้าเป็นไฟล์ Local (.mp4) */}
                        {!project.videoDemo.includes("youtube") ? (
                            <video 
                                controls 
                                className="w-full h-full object-cover"
                                poster={project.image} // ใช้รูปปกโปรเจกต์เป็นภาพตัวอย่างก่อนเล่น
                            >
                                <source src={project.videoDemo} type="video/mp4" />
                                Your browser does not support the video tag.
                            </video>
                        ) : (
                            // ถ้าเป็น YouTube (รองรับเผื่ออนาคต)
                            <iframe
                                width="100%"
                                height="100%"
                                src={project.videoDemo.replace("watch?v=", "embed/")}
                                title="YouTube video player"
                                frameBorder="0"
                                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                                allowFullScreen
                                className="absolute inset-0"
                            />
                        )}
                        
                        {/* Decor overlay for cyberpunk feel */}
                        <div className="absolute inset-0 pointer-events-none border-[1px] border-white/5 rounded-2xl"></div>
                    </div>
                </div>
            )}

            {/* 4. Architecture Diagram */}
            <div className="mb-20">
                <h3 className="text-2xl font-bold text-white mb-8 text-center">System Architecture</h3>
                <div className="bg-gray-900/50 border border-white/5 rounded-2xl p-8 md:p-12 overflow-x-auto">
                    <div className="flex flex-col md:flex-row items-center justify-between gap-8 min-w-[600px]">
                        {/* Device */}
                        <div className="flex flex-col items-center text-center gap-4">
                            <div className="w-20 h-20 bg-gray-800 rounded-2xl flex items-center justify-center border border-gray-700 shadow-[0_0_15px_rgba(0,0,0,0.5)]">
                                <Cpu className="w-10 h-10 text-orange-400" />
                            </div>
                            <div>
                                <h4 className="font-bold text-white">Machine / Sensor</h4>
                                <p className="text-xs text-gray-500">PLC, RS485, Sensors</p>
                            </div>
                        </div>
                        {/* Arrow */}
                        <div className="flex-1 h-[2px] bg-gray-700 relative w-full md:w-auto flex items-center justify-center">
                            <div className="absolute -top-3 text-[10px] text-gray-500 bg-[#050505] px-2 font-mono">Modbus / Digital I/O</div>
                            <ArrowRight className="w-4 h-4 text-gray-500 absolute right-0 -top-2" />
                        </div>
                        {/* Gateway */}
                        <div className="flex flex-col items-center text-center gap-4">
                            <div className="w-20 h-20 bg-gray-800 rounded-2xl flex items-center justify-center border border-gray-700 shadow-[0_0_15px_rgba(59,130,246,0.3)]">
                                <Wifi className="w-10 h-10 text-blue-400" />
                            </div>
                            <div>
                                <h4 className="font-bold text-white">IoT Gateway</h4>
                                <p className="text-xs text-gray-500">ESP32 / Raspberry Pi</p>
                            </div>
                        </div>
                        {/* Arrow */}
                        <div className="flex-1 h-[2px] bg-gray-700 relative w-full md:w-auto flex items-center justify-center">
                             <div className="absolute -top-3 text-[10px] text-gray-500 bg-[#050505] px-2 font-mono">MQTT / HTTP</div>
                             <ArrowRight className="w-4 h-4 text-gray-500 absolute right-0 -top-2" />
                        </div>
                        {/* Server */}
                        <div className="flex flex-col items-center text-center gap-4">
                            <div className="w-20 h-20 bg-gray-800 rounded-2xl flex items-center justify-center border border-gray-700 shadow-[0_0_15px_rgba(168,85,247,0.3)]">
                                <Database className="w-10 h-10 text-purple-400" />
                            </div>
                            <div>
                                <h4 className="font-bold text-white">Server & DB</h4>
                                <p className="text-xs text-gray-500">PostgreSQL, Next.js API</p>
                            </div>
                        </div>
                        {/* Arrow */}
                        <div className="flex-1 h-[2px] bg-gray-700 relative w-full md:w-auto flex items-center justify-center">
                             <div className="absolute -top-3 text-[10px] text-gray-500 bg-[#050505] px-2 font-mono">WebSocket</div>
                             <ArrowRight className="w-4 h-4 text-gray-500 absolute right-0 -top-2" />
                        </div>
                        {/* Client */}
                        <div className="flex flex-col items-center text-center gap-4">
                            <div className="w-20 h-20 bg-gray-800 rounded-2xl flex items-center justify-center border border-gray-700 shadow-[0_0_15px_rgba(34,197,94,0.3)]">
                                <Monitor className="w-10 h-10 text-green-400" />
                            </div>
                            <div>
                                <h4 className="font-bold text-white">Dashboard</h4>
                                <p className="text-xs text-gray-500">Admin / Operator</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            {/* 5. Business Impact */}
            <div className="mb-20 text-center">
                <h3 className="text-2xl font-bold text-white mb-8">Business Impact</h3>
                <div className="inline-block relative group cursor-default">
                    <div className="absolute inset-0 bg-green-500/20 blur-2xl rounded-full group-hover:bg-green-500/30 transition-all" />
                    <div className="relative bg-black/60 border border-green-500/30 px-12 py-8 rounded-3xl backdrop-blur-md">
                        <span className="text-3xl md:text-5xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-green-400 to-emerald-500">
                            {project.impact}
                        </span>
                    </div>
                </div>
            </div>

            {/* 6. Detail & Tech */}
            <div className="grid md:grid-cols-3 gap-12 mb-20">
                <div className="md:col-span-2 prose prose-invert prose-lg max-w-none text-gray-300">
                    <h3 className="text-2xl font-bold text-white mb-4">Project Details</h3>
                    <p className="whitespace-pre-line leading-relaxed">
                        {project.content}
                    </p>
                </div>
                <div className="bg-white/5 border border-white/10 rounded-2xl p-6 h-fit">
                    <h4 className="font-bold text-white mb-4">Technologies</h4>
                    <ul className="space-y-3">
                        {project.tech.map((t: string) => (
                            <li key={t} className="flex items-center gap-3 text-sm text-gray-400">
                                <div className="w-1.5 h-1.5 bg-blue-500 rounded-full" />
                                {t}
                            </li>
                        ))}
                    </ul>
                </div>
            </div>

            {/* 7. Gallery */}
            {project.gallery && project.gallery.length > 0 && (
                <div className="mb-24">
                     <h3 className="text-2xl font-bold text-white mb-8 border-l-4 border-blue-500 pl-4">Gallery</h3>
                     <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
                        {project.gallery.map((img: string, idx: number) => (
                            <div key={idx} className="relative h-72 rounded-2xl overflow-hidden border border-white/10 group cursor-pointer shadow-lg">
                                <Image 
                                    src={img} 
                                    alt={`Gallery ${idx}`} 
                                    fill 
                                    className="object-cover group-hover:scale-105 transition-transform duration-700 ease-out" 
                                />
                                <div className="absolute inset-0 bg-black/0 group-hover:bg-black/20 transition-colors" />
                            </div>
                        ))}
                     </div>
                </div>
            )}

            {/* 8. Next Project */}
            <div className="border-t border-white/10 pt-12">
                <p className="text-sm text-gray-500 font-mono mb-4">NEXT PROJECT</p>
                <Link href={`/projects/${nextProject.slug}`} className="group block">
                    <div className="flex items-center justify-between">
                        <div>
                            <h3 className="text-3xl md:text-5xl font-bold text-white group-hover:text-blue-400 transition-colors">
                                {nextProject.title}
                            </h3>
                            <p className="text-gray-500 mt-2 group-hover:text-gray-400 transition-colors">
                                {nextProject.tech.slice(0, 3).join(" • ")}
                            </p>
                        </div>
                        <div className="w-16 h-16 rounded-full border border-white/20 flex items-center justify-center group-hover:bg-blue-500 group-hover:border-blue-500 transition-all">
                            <ArrowRight className="w-6 h-6 text-white group-hover:translate-x-1 transition-transform" />
                        </div>
                    </div>
                </Link>
            </div>

        </div>
      </main>
    </PageTransition>
  );
}