services:
  app:
    build: .
    container_name: smart-factory-portfolio
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    volumes:
      - ./storage:/app/storage
    restart: unless-stopped
